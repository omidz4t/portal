# Changelog

## 0.1.0 (2026-08-14)

Release type: **minor**

### Features

- feat(landing): box tunnel mode like persona in the show walkthrough
- feat(landing): make /show a full-viewport walkthrough
- feat(landing): use the portal favicon and per-page social meta
- feat(landing): use mirrored cartoon-portal-render icons in the header
- feat(landing): animate PortalMark so Delta Chat walks left and Telegram teleports right
- feat(landing): extract PortalMark and mirror the far portal icon
- feat(landing): pack the site as a Delta Chat webxdc
- feat(landing): mark the bridge with mirrored Koboyo portals and Portal
- feat(landing): bounce the hero down arrow
- feat(landing): add a down arrow at the bottom of the hero
- feat(landing): open the walkthrough on its own page
- feat(landing): put a huge beaming Koboyo face on the try band
- feat(landing): open the walkthrough from a big click-me modal
- feat(landing): keep walkthrough chat height and history
- feat(landing): start pair-bot and persona stories on a chat list
- feat(landing): use Telegram and Delta Chat logos in the walkthrough
- feat(landing): start the walkthrough on a fake chat list
- feat(landing): interactive Telegram and Delta Chat tutorials
- feat(landing): mark persona as a new feature with more Koboyo icons
- feat(landing): translate all docs pages to Farsi
- feat(landing): light doodle theme, Farsi /fa, Arad font
- feat(landing): explain persona on the homepage with Koboyo icons
- feat(landing): warn about operator trust and document persona
- feat(landing): add SvelteKit marketing site

### Fixes

- fix(landing): show hamburger menu only on phones
- fix(landing): make mobile menu a full-page overlay
- fix(landing): point canonical and social URLs at omidz4t.github.io/portal
- fix(landing): clip portal slides under the gate icons
- fix(landing): drop the extra portal slot min-width
- fix(landing): give Delta Chat more room in the portal slots
- fix(landing): size portal slots to fit Delta Chat
- fix(landing): show one mirrored cartoon portal in the header
- fix(landing): clip PortalMark slides so names vanish into the gates
- fix(landing): make PortalMark names slide across, no teleport
- fix(landing): slide each name into a portal and out the other side
- fix(landing): swap Telegram and Delta Chat through the portals the same way
- fix(landing): use the portal SVG as the header logo
- fix(landing): slightly shrink PortalMark icons
- fix(landing): keep PortalMark icons large and shrink the type
- fix(landing): shrink the PortalMark lockup
- fix(landing): make the phone menu cover the full screen
- fix(landing): show Telegram and Delta Chat around the portal mark
- fix(landing): put the mirrored Portal mark in the hero
- fix(landing): make the footer background white
- fix(landing): replace the arrow character with Koboyo bridge-momentum
- fix(landing): drop monospace uppercase labels
- fix(landing): give the hero band more viewport height
- fix(landing): name persona stories Initiate and a friend writes
- fix(landing): use Tunnel mode and a bang on the English try copy
- fix(landing): make the homepage end with the footer on the try band
- fix(landing): label tunnel mode and group the persona stories
- fix(landing): Farsi try copy and mirror the beaming face
- fix(landing): keep the beaming face above the try headline
- fix(landing): put the try headline at the top of the component
- fix(landing): use a rounded rectangle behind the beaming face
- fix(landing): break the try headline after the question
- fix(landing): let the try headline use the full section width
- fix(landing): put the beaming face above the try headline on white
- fix(landing): use Koboyo very-happy-beaming on the try band
- fix(landing): make the walkthrough modal fullscreen with the paper background
- fix(landing): give Portal and BotFather different Telegram marks
- fix(landing): let phone threads fill the phone height
- fix(landing): drop logos next to the walkthrough title
- fix(landing): show the tgportal binary and keep code LTR
- fix(landing): label modes as tunnel and persona, not new
- fix(landing): use پورتال as the Farsi product name
- fix(landing): justify Farsi docs body text
- fix(landing): drop the walkthrough intro paragraph
- fix(landing): add matching top border on the trust band
- fix(landing): call the product Portal, not TGPORTAL
- fix(landing): give the example walkthrough a full-viewport stage
- fix(landing): rewrite the media section as plain cards
- fix(landing): move the example walkthrough to the end of the page
- fix(landing): put the trust warning after the hero
- fix(landing): restore valid JSON so the homepage loads
- fix(landing): restore valid fa.json after copy edit
- fix(landing): say database, not SQLite, for normal readers
- fix(landing): speak like a person, not a spec
- fix(landing): avoid capturing initial tutorial state
- fix(landing): draw the persona message path as a vertical line
- fix(landing): apply ui-ux-pro-max markdown a11y rules

